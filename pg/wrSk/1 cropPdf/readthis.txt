
PDF min:

⚠️ Important notes
1. Opening the file
You can try double-clicking index.html, but if worker fails:
👉 Run a local server:

python3 -m http.server

Then open:

http://localhost:8000

Requires:
Development tools software installed


Bash commands:

Ctr+Space > Terminal