I need static HTML page for CVS files data processing. I need a button to upload cvs file, then I need input for "Tag1", then I need txt input for "Tag2", then I need "ADD" button that will process the cvs file. When "ADD" button is clicked, if new cvs file called "report" was not created, create new cvs file called "report", and write to it for every row: Tag1, Tag2, value from "weight" column from file, value from "order num" column from file, value from "Item-sku" column from file, and value from "Tracking NO" column from file. If new file called "report" already exists, then write to that file without erasing existing rows. After every time "ADD" button is pressed display below: Tag1, Tag2, cvs file name. After every time "ADD" button is pressed clear input for Tag2 and close opened cvs file, it should be empty so if "ADD" was clicked again but there is no opened file then do nothing. Lastly, below is the txt input give name to a "report" file and a button "SAVE" to download "report" file as cvs.

note: "Tracking NO" column name in Trello is missing
note: new line breaks formatting
note: many empty rows

⚠️ Notes / Limitations
This is fully client-side, so:
It cannot truly “persist” files between page reloads
If you refresh, the report is lost (unless you want localStorage version—I can add that)
🚀 Optional upgrades (I can add if you want)
Drag & drop CSV upload
Better CSV parser (handles quoted commas)
Persist report in browser (localStorage)
Preview table before saving
Validation (missing columns warning)
